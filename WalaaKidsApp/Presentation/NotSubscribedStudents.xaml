<UserControl x:Class="WalaaKidsApp.Presentation.NotSubscribedStudents"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WalaaKidsApp.Presentation"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             x:Name="NotSubscribedStudentsUserControl"
             FlowDirection="RightToLeft"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             Background="Transparent"
             Opacity="0"
             d:Opacity="1"
             d:DesignWidth="800" d:DesignHeight="800"
             d:Background="#eff2f7" Loaded="NotSubscribedStudentsUserControl_Loaded"
             PreviewKeyDown="NotSubscribedStudentsUserControl_PreviewKeyDown"
>
    <UserControl.Resources>
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation 
              Storyboard.TargetProperty="Opacity"
         Storyboard.TargetName="NotSubscribedStudentsUserControl"
              From="0" To="1"
              Duration="0:0:0.4" />
        </Storyboard>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border BorderThickness="0 0 0 2">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#662D8C" Offset="1"/>
                    <GradientStop Color="#ED1E79" Offset=".1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Grid 
                 HorizontalAlignment="Center"
                 VerticalAlignment="Top"
                 Grid.Row="0"
                 Width="270"
      >
                <TextBlock FontWeight="SemiBold" 
                    Text="بحث :"
                    VerticalAlignment="Center"
             />
                <TextBox x:Name="KeywordTextBox" Width="200" Height="35" Padding="10 8 30 0" 
                         d:Text="زين على جمال على محمد فرغلى"/>

                <Button Name="SearchButton" Click="SearchButton_Click"
                        Style="{StaticResource SearchButton}" HorizontalAlignment="Right" Margin="0 0 40 0">
                    <icon:PackIconMaterial Kind="Magnify" Width="25" Height="20">
                        <icon:PackIconMaterial.Foreground>
                            <LinearGradientBrush>
                                <GradientStop Color="#662D8C" Offset="1"/>
                                <GradientStop Color="#ED1E79" Offset=".1"/>
                            </LinearGradientBrush>
                        </icon:PackIconMaterial.Foreground>
                    </icon:PackIconMaterial>
                </Button>
            </Grid>
        </Border>
        <DataGrid Style="{DynamicResource DataGridStyle1}" 
                  CellStyle="{DynamicResource DataGridCellStyle1}" 
                  ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" 
                  RowStyle="{DynamicResource DataGridRowStyle1}" 
                  Grid.Row="1"
                  Margin="5"
                  IsReadOnly="True"
                  AutoGenerateColumns="False"
                  VerticalScrollBarVisibility="Auto"
                  HeadersVisibility="Column"
                  ItemsSource="{Binding Students}"
                  SelectedItem="{Binding SelectedStudent}"
                  HorizontalContentAlignment="Center"
                  
                  >

            <DataGrid.Columns>
                <DataGridTextColumn Header="الرقم" Width="auto" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="الاسم" Width="auto" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="تليفون" Width="auto" Binding="{Binding Guardian.PhoneNumber}"/>
                <DataGridTemplateColumn Width="auto" Header="خيارات">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Width="auto" Orientation="Horizontal" VerticalAlignment="Center"
                                        HorizontalAlignment="Center">

                                <Button x:Name="RenewSubscriptionButton" Style="{StaticResource SearchButton}" ToolTip="تجديد الاشتراك" Click="RenewSubscriptionButton_Click" >
                                    <icon:PackIconMaterial Kind="Autorenew" Foreground="#ED1E79"/>
                                </Button>
                                <Button x:Name="StudentDetailsButton" Style="{StaticResource SearchButton}" Click="StudentDetailsButton_Click"
                                        ToolTip="تفاصيل">
                                    <icon:PackIconMaterial Kind="ListBox" Foreground="#ED1E79"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
