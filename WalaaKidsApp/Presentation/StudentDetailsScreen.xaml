<UserControl x:Class="WalaaKidsApp.Presentation.StudentDetailsScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:local="clr-namespace:WalaaKidsApp.Presentation"
             mc:Ignorable="d" 
             x:Name="StudentDetailsUserControl"
             FlowDirection="RightToLeft"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             Background="Transparent"
             Opacity="1"
             d:Opacity="1"
             d:DesignWidth="800" d:DesignHeight="800"
             d:Background="#eff2f7">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" BorderThickness="0 0 0 2">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#662D8C" Offset="1"/>
                    <GradientStop Color="#ED1E79" Offset=".1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <TextBlock Style="{StaticResource InputLabel}" Text="اسم الطالب:" 
                       HorizontalAlignment="Right"
                       FontSize="15"
                       Grid.Row="0" Grid.Column="0"
                       />
                <TextBlock x:Name="NameTextBlock" Style="{StaticResource InputLabel}" d:Text="زين على جمال على محمد" 
                       HorizontalAlignment="Left"
                       FontSize="15"
                       Grid.Row="0" Grid.Column="1"
                       />
                <TextBlock Style="{StaticResource InputLabel}" Text="الفصل:" 
                     HorizontalAlignment="Right"
                     FontSize="15"
                     Grid.Row="1" Grid.Column="0"
                     />
                <TextBlock Name="ClassroomTextBlock" Style="{StaticResource InputLabel}" d:Text="Yellow" 
                     HorizontalAlignment="Left"
                     FontSize="15"
                     Grid.Row="1" Grid.Column="1"
                     />
            </Grid>
        </Border>
        <TabControl Background="Transparent" Grid.Row="1">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" 
                                        Background="Transparent" 
                                        BorderBrush="#ED1E79" 
                                        BorderThickness="1"
                                        CornerRadius="2"
                                        Padding="4,2"
                                        >
                                    <ContentPresenter x:Name="ContentSite"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"
                                      ContentSource="Header"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="White"/>
                                        <Setter TargetName="Border" Property="BorderBrush" Value="DarkGray"/>
                                        <Setter Property="Height" Value="30"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter Property="Height" Value="25"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabItem Name="StudentDetailsTab" Header="بيانات الطالب"/>
            <TabItem Name="StudentSubscriptionDetailsTab" Header="الأشتراك الحالي"/>
            <TabItem Name="StudentOldSubscriptionsDetailsTab" Header="الاشتراكات السابقة"/>
        </TabControl>
    </Grid>
</UserControl>
